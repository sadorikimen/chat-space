 .message
  .message__upper-info
    %p.message__upper-info__talker 
      = @group.user.name
    %p.message__upper-info__date 
      = @group.created_at.strftime("%Y年%m月%d日 %H時%M分")
  %p.message__text 
    - if @group.content.present?
      .message__text
        = @group.content
    = image_tag @group.image.url, class: 'message__image' if message.image.present?